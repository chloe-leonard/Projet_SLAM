
{# Rendre le formulaire principal #}
{{ form_start(form) }}
    {{ form_widget(form) }}
<!--
<h3>Hashtags</h3>
<ul class="hashtags-collection">
    { for hashtag in form.hashtags }
        <li class="hashtag-item">
            { form_row(hashtag) }
        </li>
    { endfor }
    <li>
        <button type="button" class="bouton-ajouter-hashtag">Ajouter un hashtag</button>
    </li>

</ul>
-->
<button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}


<!-- Ajout de la collection de hashtags
{% block javascripts %}
<script>
    const boutonAjouterHashtag = document.querySelector(".bouton-ajouter-hashtag");
    const zoneHashtags = document.querySelector(".hashtags-collection");

    boutonAjouterHashtag.addEventListener("click", ajouterHashtag);

    zoneHashtags.addEventListener("click", (event) => {
        const buttonClicked = event.target;

        if (buttonClicked.classList.contains("bouton-supprimer-hashtag")) {
            const hashtagItemToDelete = buttonClicked.parentElement;
            if (hashtagItemToDelete) {
                zoneHashtags.removeChild(hashtagItemToDelete);
            } else {
                console.error("Élément hashtag introuvable pour la suppression.");
            }
        }
    });

    function ajouterHashtag() {
        // Créer un élément input de type "text"
        const inputHashtag = document.createElement("input");
        inputHashtag.setAttribute("type", "text");
        inputHashtag.setAttribute("class", "hashtag-input");
        inputHashtag.setAttribute("placeholder", "Entrez un hashtag");

        // Créer un élément div pour contenir le champ hashtag
        const divHashtag = document.createElement("div");
        divHashtag.classList.add("hashtag-container");
        divHashtag.appendChild(inputHashtag);

        // Créer un élément button pour supprimer le champ hashtag
        const boutonSupprimerHashtag = document.createElement("button");
        boutonSupprimerHashtag.setAttribute("type", "button");
        boutonSupprimerHashtag.setAttribute("class", "bouton-supprimer-hashtag");
        boutonSupprimerHashtag.textContent = "Supprimer";
        divHashtag.appendChild(boutonSupprimerHashtag);

        // Ajouter la nouvelle div hashtag à la zone de saisie
        zoneHashtags.appendChild(divHashtag);
    }

// Verifications a l'appui sur le bouton submit
    // Vérifier si le hashtag commence par '#'
//    if (!hashtag.startsWith("#")) {
//      return false;
//   }

</script>
{% endblock %}
-->